export const typeList = ['work', 'home', 'personal'];

export const phoneNumberRegexp = /^\+?[\d\s\-()]{3,20}$/; // Checks if the string is a phone number that can start with an optional "+", contains only digits, spaces, dashes, or parentheses, and is 3 to 20 characters long
